
@model POA.Web.Models.Country.CountryDetailsViewModel

@{
    ViewBag.Country = Model.Name;
}



<div class="well">
    <p class="lead">
        @Model.Name
    </p>

    <p class="lead">
        @Model.Description
    </p>
</div>
<div>
    @using (Html.BeginForm("Delete", "Country", new { id = Model.Country_ID }, FormMethod.Post, new { id = "delete-form" }))
    {
        @Html.AntiForgeryToken()
        @Html.ActionLink("Edit Country", "Edit", "Country", new { id = Model.Country_ID }, new { @class = "btn btn-primary" })
        <button type="submit" class="btn btn-warning">Delete Country</button>
        @Html.ActionLink("Cancel", "Cancel", "Country", new { @class = "btn btn-primary" })
    }

</div>



@section scripts
{
    <script>
        $(function () {
            $("#delete-form").on("submit", function () {
                return confirm("Are you sure you wish to delete this Country?  You cannot undo this action.");
            });
        });
    </script>
}
